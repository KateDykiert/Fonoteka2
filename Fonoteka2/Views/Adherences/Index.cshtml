@model IEnumerable<Fonoteka2.Models.Przynaleznosc>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Playlista.Nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Utwor.Tytul)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Playlista.Nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Utwor.Tytul)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPrzynaleznosci }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPrzynaleznosci }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPrzynaleznosci })
        </td>
    </tr>
}

</table>
