@model IEnumerable<Fonoteka2.Models.Umiejetnosc>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Instrument.Nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Wykonawca.Imie)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Instrument.Nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Wykonawca.Imie)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdUmiejetnosci }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdUmiejetnosci }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdUmiejetnosci })
        </td>
    </tr>
}

</table>
